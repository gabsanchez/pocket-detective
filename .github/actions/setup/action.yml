name: Setup
description: Install Node.js, install dependencies, and generate Prisma client

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version-file: '.nvmrc'
        cache: npm

    - run: npm ci
      shell: bash

    - run: npx prisma generate
      shell: bash
      env:
        DATABASE_URL: postgresql://user:pass@localhost:5432/testdb
